/*
* Contains the logic that deals with learning situation actions
* sent by game instances.
*/
define([
  'Debug'
], function(
  Debug
) {

  function LearningCore(args) {
  };

  return LearningCore;
});
